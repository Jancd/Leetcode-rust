```rust
impl Solution {
    pub fn search(nums: Vec<i32>, target: i32) -> bool {
        for val in nums {
            if val == target {
                return true;
            }
        }
        false
    }
}
```
